## 目标

* 将“货币基金信息”（年化利率、起始日期）移入【编辑资产】弹窗，不在详情页单独展示输入区域。

* 调整详情页模块顺序：1 资产详情 → 2 收益概览 → 3 资产变更详情 → 4 持仓管理。

* 保持持仓管理只对 `stock`/`fund` 类型显示；`huobi`（货币基金）不显示持仓管理，在货币基金中，直接将持仓管理这项隐藏掉。

## 类型识别

* 在页面中保持/完善货币基金识别逻辑：当 `type.code` 为 `huobi` 或 `money_fund`，或 `type.label` 包含“货币基金”时，视为货币基金。

* 位置：`app/assets/[id]/page.tsx` 中定义 `isMoneyFund`（与现有 `isStockOrFund` 并存）。

## 详情页布局调整

* 移除“货币基金信息”卡片（输入与保存按钮的独立区域）。

* 重新排列组件顺序：

  * 第一块：资产详情卡片（不变）。

  * 第二块：收益概览卡片（移动到资产详情之后）。

  * 第三块：资产变更详情卡片（移动到收益概览之后）。

  * 第四块：持仓管理卡片（最后；仅对 `stock`/`fund` 显示）。

* 文件与代码：`app/assets/[id]/page.tsx`，删除原基金信息卡片的 JSX（原约 `125-149` 行），并移动收益概览与资产变更的 JSX 顺序。

## 编辑资产弹窗改造

* 将年化利率、起始日期两个输入项加入【编辑资产】弹窗，仅在 `isMoneyFund` 为真时显示。

* 编辑态类型扩展：把 `assetEditing` 的类型从 `Asset` 扩展为 `FundAsset`，以便读写 `annualRate`、`startDate`。

* 预填表单：打开弹窗时使用当前资产的 `annualRate` 与 `startDate` 预填（日期格式 `YYYY-MM-DD`）。

* 保存请求：在 PUT `/api/assets/:id` 的 payload 中包含 `annualRate` 与 `startDate`（仅在货币基金时传递；其余类型不传或传 `undefined`）。

* 位置：`app/assets/[id]/page.tsx` 的编辑弹窗区域（约 `256-283` 与 `259-271`）。

## 接口与数据

* 后端无需新增接口；沿用现有 `PUT /api/assets/:id`（Zod 已支持 `annualRate`、`startDate`）。

* 保持金额/估值日期等其余字段逻辑不变。

## 验证

* 手动测试流程：

  * 打开一个 `huobi` 类型资产详情，确认页面顺序为：资产详情 → 收益概览 → 资产变更 → 持仓管理（不显示）。

  * 点击【编辑资产】，看到“年化利率/起始日期”两个输入项；修改后点击保存，刷新后收益概览正确计算显示。

  * 对非货币基金资产，编辑弹窗不显示上述两个输入项，功能不受影响。

如确认该计划，我将开始实施上述改动并自检。
