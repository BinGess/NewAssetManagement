## 目标
- 移除固定支出页上方筛选区域（当前页面已无筛选区，无需改动）
- 在类型管理页新增“所属人”管理（已实现）：支持新增、编辑、删除，搜索与启用状态筛选、排序持久化
- 在固定支出页新增“每月固定开支”概述：显示 年度固定支出/12 + 所有月度开支 的合计

## 实施
- Expenses 页面：
  - 计算 `monthlyOverview = sum(monthly) + sum(yearly)/12`
  - 在新增表单卡片下方插入概述卡片，金额使用 `formatAmount`
- Types 页面：已包含“所属人管理”卡片与排序/筛选，无需改动

## 验收
- 固定支出页顶部显示“每月固定开支：XX元”并随数据实时更新
- 类型管理页“所属人”管理功能完整可用