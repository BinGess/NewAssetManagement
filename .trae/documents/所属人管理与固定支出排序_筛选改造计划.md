## 目标
- 在“类型管理”页新增“所属人”管理卡片：支持新增、编辑、启用/禁用、删除，表格排序与搜索筛选
- 在“固定支出”页增强排序与筛选：按所属人、周期、日期范围、名称搜索；排序偏好持久化

## 后端接口（沿用现有）
- 已有：`GET/POST /api/persons`、`PUT/DELETE /api/persons/:id`
- 已有：`GET/POST /api/expenses`、`GET/PUT/DELETE /api/expenses/:id`
- 本次筛选先用前端过滤（客户端筛选），后续可扩展服务端查询参数（personId/cycle/dateFrom/dateTo/name）

## 类型管理页（/types）改造
- 新增“所属人”卡片：
  - 顶部新增表单：姓名（必填）、启用开关；提交新增
  - 表格列：姓名、启用、操作（编辑/删除）
  - 操作：
    - 编辑弹窗：修改姓名与启用状态；提交 `PUT /api/persons/:id`
    - 删除弹窗：确认后 `DELETE`；若被引用返回 400，顶部 Alert 提示“存在关联支出，不可删除”
  - 排序：表头“姓名”支持 asc/desc 切换；持久化到 `localStorage('persons_sort')`
  - 搜索筛选：
    - 关键词搜索（姓名包含）
    - 启用状态筛选（全部/启用/禁用）

## 固定支出页（/expenses）增强
- 筛选区（列表上方）：
  - 所属人下拉（来自 `/api/persons`）
  - 周期下拉（daily/weekly/monthly/yearly/全部）
  - 日期范围（起始/结束）
  - 名称搜索输入
  - 重置筛选按钮
- 列表数据：在前端基于当前筛选条件过滤，再按当前排序显示；金额继续采用友好格式
- 排序：延续现有排序（名称/周期/金额/日期），偏好已持久化（`expenses_sort`）

## 组件与样式
- 复用 `Card/Button/Input/Select/Modal/Alert` 简约风；新增小型开关（可用按钮替代）
- 错误与成功反馈统一；删除弹窗二次确认

## 实施步骤
1. /types：新增“所属人”卡片与增删改 UI、排序与搜索筛选、状态切换
2. /expenses：添加筛选区与前端过滤逻辑；保留现有排序与持久化
3. 验证：本地数据联动（新增所属人后可在支出页选择），删除被引用人返回 400 并提示

## 验收标准
- 所属人管理可新增/编辑/启用/禁用/删除；排序与搜索筛选可用且持久化排序
- 固定支出页筛选（所属人/周期/日期范围/名称）与排序协同工作；重置后回到默认视图

若确认，我将马上实现上述 UI 与逻辑改造。